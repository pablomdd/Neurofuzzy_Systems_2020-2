%-----------Practice 03-------
%-by @pablomdd

%Plot the solution of Tnorm and Snorm operations for the following fuzzy
%sets
X = -15:0.1:15;
A = zeros(1,length(X));
B = zeros(1,length(X));

for i = 1:length(A)
    
end


%------------Tnorm ops------------
figure(1)